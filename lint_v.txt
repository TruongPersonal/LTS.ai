
> lts.ai@0.0.0 lint
> oxlint


  ! react(set-state-in-effect): Calling setState synchronously within an effect can trigger cascading renders
    ,-[src/hooks/useSubtitleTrack.ts:10:7]
  7 | 
  8 |   useEffect(() => {
    :   ^^^^|^^^^
    :       `-- This is the containing effect
  9 |     if (subtitles.length === 0) {
 10 |       setSubtitleTrackUrl(undefined);
    :       ^^^^^^^^^|^^^^^^^^^
    :                `-- Avoid calling setState() directly within an effect
 11 |       return;
    `----
  help: Effects should synchronize React with external systems. Calling setState synchronously inside an effect starts another render and is usually unnecessary. Derive the value during render, initialize state directly, or update it from the event that caused the change. Use an effect only when synchronizing with an external system.
  note: React Compiler skipped optimizing this component or hook. Additional guidance: https://react.dev/reference/eslint-plugin-react-hooks/lints/set-state-in-effect

  ! react(set-state-in-effect): Calling setState synchronously within an effect can trigger cascading renders
    ,-[src/components/media/RenameFileModal.tsx:25:7]
 22 | 
 23 |   useEffect(() => {
    :   ^^^^|^^^^
    :       `-- This is the containing effect
 24 |     if (file) {
 25 |       setNameDraft(file.file_name);
    :       ^^^^^^|^^^^^
    :             `-- Avoid calling setState() directly within an effect
 26 |     }
    `----
  help: Effects should synchronize React with external systems. Calling setState synchronously inside an effect starts another render and is usually unnecessary. Derive the value during render, initialize state directly, or update it from the event that caused the change. Use an effect only when synchronizing with an external system.
  note: React Compiler skipped optimizing this component or hook. Additional guidance: https://react.dev/reference/eslint-plugin-react-hooks/lints/set-state-in-effect

  ! react(set-state-in-effect): Calling setState synchronously within an effect can trigger cascading renders
    ,-[src/hooks/useProjects.ts:35:10]
 33 | 
 34 |   useEffect(() => {
    :   ^^^^|^^^^
    :       `-- This is the containing effect
 35 |     void loadProjects();
    :          ^^^^^^|^^^^^
    :                `-- Avoid calling setState() directly within an effect
 36 |   }, [loadProjects]);
    `----
  help: Effects should synchronize React with external systems. Calling setState synchronously inside an effect starts another render and is usually unnecessary. Derive the value during render, initialize state directly, or update it from the event that caused the change. Use an effect only when synchronizing with an external system.
  note: React Compiler skipped optimizing this component or hook. Additional guidance: https://react.dev/reference/eslint-plugin-react-hooks/lints/set-state-in-effect

  ! react(set-state-in-effect): Calling setState synchronously within an effect can trigger cascading renders
    ,-[src/components/projects/EditProjectModal.tsx:15:36]
 14 |   const [errorMsg, setErrorMsg] = useState<string | null>(null);
 15 |   useEffect(() => { if (project) { setTitle(project.title); setDescription(project.description || ''); setErrorMsg(null); } }, [project]);
    :   ^^^^|^^^^                        ^^^^|^^^
    :       |                                `-- Avoid calling setState() directly within an effect
    :       `-- This is the containing effect
 16 |   if (!isOpen || !project) return null;
    `----
  help: Effects should synchronize React with external systems. Calling setState synchronously inside an effect starts another render and is usually unnecessary. Derive the value during render, initialize state directly, or update it from the event that caused the change. Use an effect only when synchronizing with an external system.
  note: React Compiler skipped optimizing this component or hook. Additional guidance: https://react.dev/reference/eslint-plugin-react-hooks/lints/set-state-in-effect

  ! react(set-state-in-effect): Calling setState synchronously within an effect can trigger cascading renders
    ,-[src/pages/DashboardPage.tsx:41:35]
 38 | 
 39 |   useEffect(() => {
    :   ^^^^|^^^^
    :       `-- This is the containing effect
 40 |     if (!intent) return;
 41 |     if (intent.type === 'create') setIsCreateOpen(true);
    :                                   ^^^^^^^|^^^^^^^
    :                                          `-- Avoid calling setState() directly within an effect
 42 |     if (intent.type === 'search') {
    `----
  help: Effects should synchronize React with external systems. Calling setState synchronously inside an effect starts another render and is usually unnecessary. Derive the value during render, initialize state directly, or update it from the event that caused the change. Use an effect only when synchronizing with an external system.
  note: React Compiler skipped optimizing this component or hook. Additional guidance: https://react.dev/reference/eslint-plugin-react-hooks/lints/set-state-in-effect

  ! react(set-state-in-effect): Calling setState synchronously within an effect can trigger cascading renders
     ,-[src/hooks/useEditorVideo.ts:109:10]
 107 | 
 108 |   useEffect(() => {
     :   ^^^^|^^^^
     :       `-- This is the containing effect
 109 |     void loadVideo();
     :          ^^^^|^^^^
     :              `-- Avoid calling setState() directly within an effect
 110 |     return () => {
     `----
  help: Effects should synchronize React with external systems. Calling setState synchronously inside an effect starts another render and is usually unnecessary. Derive the value during render, initialize state directly, or update it from the event that caused the change. Use an effect only when synchronizing with an external system.
  note: React Compiler skipped optimizing this component or hook. Additional guidance: https://react.dev/reference/eslint-plugin-react-hooks/lints/set-state-in-effect

  ! react(set-state-in-effect): Calling setState synchronously within an effect can trigger cascading renders
    ,-[src/hooks/useProjectFiles.ts:40:10]
 38 | 
 39 |   useEffect(() => {
    :   ^^^^|^^^^
    :       `-- This is the containing effect
 40 |     void loadFiles();
    :          ^^^^|^^^^
    :              `-- Avoid calling setState() directly within an effect
 41 |   }, [loadFiles]);
    `----
  help: Effects should synchronize React with external systems. Calling setState synchronously inside an effect starts another render and is usually unnecessary. Derive the value during render, initialize state directly, or update it from the event that caused the change. Use an effect only when synchronizing with an external system.
  note: React Compiler skipped optimizing this component or hook. Additional guidance: https://react.dev/reference/eslint-plugin-react-hooks/lints/set-state-in-effect

  ! react(set-state-in-effect): Calling setState synchronously within an effect can trigger cascading renders
     ,-[src/AppRoutes.tsx:177:21]
 175 | 
 176 |   useEffect(() => {
     :   ^^^^|^^^^
     :       `-- This is the containing effect
 177 |     if (intentType) setIntentId((id) => id + 1);
     :                     ^^^^^|^^^^^
     :                          `-- Avoid calling setState() directly within an effect
 178 |   }, [intentType]);
     `----
  help: Effects should synchronize React with external systems. Calling setState synchronously inside an effect starts another render and is usually unnecessary. Derive the value during render, initialize state directly, or update it from the event that caused the change. Use an effect only when synchronizing with an external system.
  note: React Compiler skipped optimizing this component or hook. Additional guidance: https://react.dev/reference/eslint-plugin-react-hooks/lints/set-state-in-effect

  ! react(set-state-in-effect): Calling setState synchronously within an effect can trigger cascading renders
     ,-[src/AppRoutes.tsx:190:7]
 179 | 
 180 |   useEffect(() => {
     :   ^^^^|^^^^
     :       `-- This is the containing effect
 181 |     if (!checkoutStatus) return;
 182 | 
 183 |     const checkoutKey = `${checkoutStatus}:${checkoutSessionId || ''}`;
 184 |     if (handledCheckoutRef.current === checkoutKey) return;
 185 |     handledCheckoutRef.current = checkoutKey;
 186 | 
 187 |     const clearCheckoutParams = () => navigate('/projects', { replace: true });
 188 | 
 189 |     if (checkoutStatus === 'cancelled') {
 190 |       setCheckoutNotice({ type: 'error', message: t('subscription.checkoutCancelled') });
     :       ^^^^^^^^|^^^^^^^^
     :               `-- Avoid calling setState() directly within an effect
 191 |       clearCheckoutParams();
     `----
  help: Effects should synchronize React with external systems. Calling setState synchronously inside an effect starts another render and is usually unnecessary. Derive the value during render, initialize state directly, or update it from the event that caused the change. Use an effect only when synchronizing with an external system.
  note: React Compiler skipped optimizing this component or hook. Additional guidance: https://react.dev/reference/eslint-plugin-react-hooks/lints/set-state-in-effect

  ! react(set-state-in-effect): Calling setState synchronously within an effect can trigger cascading renders
     ,-[src/AppRoutes.tsx:246:5]
 242 | 
 243 |   useEffect(() => {
     :   ^^^^|^^^^
     :       `-- This is the containing effect
 244 |     if (!projectId) return;
 245 |     let mounted = true;
 246 |     setLoading(true);
     :     ^^^^^|^^^^
     :          `-- Avoid calling setState() directly within an effect
 247 |     setError(null);
     `----
  help: Effects should synchronize React with external systems. Calling setState synchronously inside an effect starts another render and is usually unnecessary. Derive the value during render, initialize state directly, or update it from the event that caused the change. Use an effect only when synchronizing with an external system.
  note: React Compiler skipped optimizing this component or hook. Additional guidance: https://react.dev/reference/eslint-plugin-react-hooks/lints/set-state-in-effect

  ! react(set-state-in-effect): Calling setState synchronously within an effect can trigger cascading renders
     ,-[src/AppRoutes.tsx:316:5]
 312 | 
 313 |   useEffect(() => {
     :   ^^^^|^^^^
     :       `-- This is the containing effect
 314 |     if (!projectId || !fileId) return;
 315 |     let mounted = true;
 316 |     setLoading(true);
     :     ^^^^^|^^^^
     :          `-- Avoid calling setState() directly within an effect
 317 |     setError(null);
     `----
  help: Effects should synchronize React with external systems. Calling setState synchronously inside an effect starts another render and is usually unnecessary. Derive the value during render, initialize state directly, or update it from the event that caused the change. Use an effect only when synchronizing with an external system.
  note: React Compiler skipped optimizing this component or hook. Additional guidance: https://react.dev/reference/eslint-plugin-react-hooks/lints/set-state-in-effect

  ! react(set-state-in-effect): Calling setState synchronously within an effect can trigger cascading renders
    ,-[src/hooks/useEditorSubtitles.ts:59:10]
 57 | 
 58 |   useEffect(() => {
    :   ^^^^|^^^^
    :       `-- This is the containing effect
 59 |     void loadSubtitles();
    :          ^^^^^^|^^^^^^
    :                `-- Avoid calling setState() directly within an effect
 60 |   }, [loadSubtitles]);
    `----
  help: Effects should synchronize React with external systems. Calling setState synchronously inside an effect starts another render and is usually unnecessary. Derive the value during render, initialize state directly, or update it from the event that caused the change. Use an effect only when synchronizing with an external system.
  note: React Compiler skipped optimizing this component or hook. Additional guidance: https://react.dev/reference/eslint-plugin-react-hooks/lints/set-state-in-effect

  ! react(set-state-in-effect): Calling setState synchronously within an effect can trigger cascading renders
    ,-[src/pages/AdminPage.tsx:72:10]
 70 | 
 71 |   useEffect(() => {
    :   ^^^^|^^^^
    :       `-- This is the containing effect
 72 |     void loadOverview();
    :          ^^^^^^|^^^^^
    :                `-- Avoid calling setState() directly within an effect
 73 |   }, [loadOverview]);
    `----
  help: Effects should synchronize React with external systems. Calling setState synchronously inside an effect starts another render and is usually unnecessary. Derive the value during render, initialize state directly, or update it from the event that caused the change. Use an effect only when synchronizing with an external system.
  note: React Compiler skipped optimizing this component or hook. Additional guidance: https://react.dev/reference/eslint-plugin-react-hooks/lints/set-state-in-effect

  ! react(set-state-in-effect): Calling setState synchronously within an effect can trigger cascading renders
    ,-[src/pages/AdminPage.tsx:76:10]
 74 | 
 75 |   useEffect(() => {
    :   ^^^^|^^^^
    :       `-- This is the containing effect
 76 |     void loadUsers();
    :          ^^^^|^^^^
    :              `-- Avoid calling setState() directly within an effect
 77 |   }, [loadUsers]);
    `----
  help: Effects should synchronize React with external systems. Calling setState synchronously inside an effect starts another render and is usually unnecessary. Derive the value during render, initialize state directly, or update it from the event that caused the change. Use an effect only when synchronizing with an external system.
  note: React Compiler skipped optimizing this component or hook. Additional guidance: https://react.dev/reference/eslint-plugin-react-hooks/lints/set-state-in-effect

  ! react(set-state-in-effect): Calling setState synchronously within an effect can trigger cascading renders
     ,-[src/components/editor/VideoPlayer.tsx:143:5]
 141 | 
 142 |   useEffect(() => {
     :   ^^^^|^^^^
     :       `-- This is the containing effect
 143 |     setPlaybackError(null);
     :     ^^^^^^^^|^^^^^^^
     :             `-- Avoid calling setState() directly within an effect
 144 |     setCurrentTime(0);
     `----
  help: Effects should synchronize React with external systems. Calling setState synchronously inside an effect starts another render and is usually unnecessary. Derive the value during render, initialize state directly, or update it from the event that caused the change. Use an effect only when synchronizing with an external system.
  note: React Compiler skipped optimizing this component or hook. Additional guidance: https://react.dev/reference/eslint-plugin-react-hooks/lints/set-state-in-effect

Found 15 warnings and 0 errors.
Finished in 51ms on 104 files with 116 rules using 12 threads.
